# Dim direnv status messages
export DIRENV_LOG_FORMAT=$'\033[2mdirenv: %s\033[0m'

# Manually load direnv.
nix_direnv_manual_reload

# Use flake
use flake

# Watch additional files
watch_file draw/default.nix
